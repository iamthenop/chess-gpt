"""
Exact-position query helpers.

The first usable exact-query path currently lives in query.suggest.
This module is reserved for future lower-level exact lookup helpers if
they separate cleanly from move suggestion formatting.
"""
